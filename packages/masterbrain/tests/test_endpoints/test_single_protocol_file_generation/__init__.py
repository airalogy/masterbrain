"""
Tests for Protocol Generation V3 endpoint.

This module tests the unified protocol generation functionality (V3) where
protocol.aimd and assigner code are combined into a single file with
embedded ```assigner code blocks.
"""
